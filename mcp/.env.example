ARCH_API_BASE_URL=https://arch-ai-tools.onrender.com
ARCH_API_KEY=at_sk_your_agent_key_here

# Transport: "stdio" (default, for CLI clients) or "sse" (for web/hosted agents)
MCP_TRANSPORT=stdio

# SSE port (only used when MCP_TRANSPORT=sse)
MCP_SSE_PORT=3001
